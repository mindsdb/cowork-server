# Snyk (https://snyk.io) policy file, patches or ignores known vulnerabilities.
version: v1.25.0
# No ignores. An entry belongs here only when a finding has no upstream fix;
# anything with a released fix is a base-image bump, not an exception.
ignore: {}
patch: {}
